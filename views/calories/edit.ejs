<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<% if (user) { %>
  <h1 class="new-meal-title"><%= title %></h1>
  
  
  <form class="no-data" action="/profile/calories/<%= user?.profile._id %>?_method=PATCH" method="post">
    <input type="number" name="calorieLimit" placeholder="Calorie Budget">
    <button class="button-data" type="submit">Save</button>
  </form>

  <% } else { %>
  
    <h1>Please Log in to Continue</h1>

  <% } %>
  

<%- include('../partials/footer') %>